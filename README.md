PhatBak is a file backup utility for Linux and similar operating systems.

Features:
    Easy to use
    Automatic incremental backups
    Uses hard links to reduce redundant data storage
    Written in compiled language (C++)
    Multi-threaded
    Archive format in human-readable form
